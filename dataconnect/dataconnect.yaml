specVersion: "v1beta"
serviceId: "netconf"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "netconf-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
